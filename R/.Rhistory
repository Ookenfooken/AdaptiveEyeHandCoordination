library("R.matlab")
# set directory
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
# load in data
coordination = data.frame(readMat("spatiotemporalCoordination.mat"))
colnames(coordination) <- c("participant", "testID", "tool", "dual",
"gazeShift.ballSlot", "gazeshift.slotDisplay")
coordination$participant <- as.factor(coordination$participant)
coordination$testID <- NULL #redundant
coordination$tool <- as.factor(coordination$tool)
coordination$dual <- as.factor(coordination$dual)
# aggregate data per condition and participant
gazeShifts <- aggregate(. ~ participant + tool + dual, median, na.rm = TRUE, na.action = NULL, data = coordination)
# t-test for single task condition
gazeShifts_single <- gazeShifts[gazeShifts$dual == 0,]
t.test(gazeShifts_single[gazeShifts_single$tool == 1,]$gazeShift.ballSlot, gazeShifts_single[gazeShifts_single$tool == 0,]$gazeShift.ballSlot,
paired = TRUE)
mean(gazeShifts_single[gazeShifts_single$tool == 1,]$gazeShift.ballSlot)
mean(gazeShifts_single[gazeShifts_single$tool == 0,]$gazeShift.ballSlot)
# mean and sd fingertips
mean(gazeShifts_single[gazeShifts_single$tool == 0,]$gazeShift.ballSlot)
sd(gazeShifts_single[gazeShifts_single$tool == 0,]$gazeShift.ballSlot)
# mean and sd fingertips
mean(gazeShifts_single[gazeShifts_single$tool == 0,]$gazeShift.ballSlot)/200
sd(gazeShifts_single[gazeShifts_single$tool == 0,]$gazeShift.ballSlot)/200
# mean & sd tweezers
mean(gazeShifts_single[gazeShifts_single$tool == 1,]$gazeShift.ballSlot)/200
sd(gazeShifts_single[gazeShifts_single$tool == 1,]$gazeShift.ballSlot)/200
# mean and sd fingertips in ms
mean(gazeShifts_single[gazeShifts_single$tool == 0,]$gazeShift.ballSlot)
sd(gazeShifts_single[gazeShifts_single$tool == 0,]$gazeShift.ballSlot)
# mean & sd tweezers in ms
mean(gazeShifts_single[gazeShifts_single$tool == 1,]$gazeShift.ballSlot)
sd(gazeShifts_single[gazeShifts_single$tool == 1,]$gazeShift.ballSlot)
rm(list = ls())
# load in data
phaseDurations = data.frame(readMat("phaseDurations.mat"))
colnames(phaseDurations) <- c("participant", "testID", "tool", "dual",
"reach", "ballApproach", "ballGrasp", "transport",
"slotApproach", "slotEntry", "return")
phaseDurations$participant <- as.factor(phaseDurations$participant)
phaseDurations$testID <- NULL #redundant
phaseDurations$tool <- as.factor(phaseDurations$tool)
phaseDurations$dual <- as.factor(phaseDurations$dual)
# Anova Result
## $ANOVA
## Effect DFn DFd         F            p p<.05        ges
## 2      tool   1  10 41.345849 7.537003e-05     * 0.46528096
## 3      dual   1  10  1.763796 2.136658e-01       0.05934917
## 4 tool:dual   1  10  2.145116 1.737421e-01       0.02119980
# Because there is no effect of task load, we will compare phase duration for grasp modes separately
phases_compare <- aggregate(duration ~ participant + tool, mean, na.rm = TRUE, na.action = NULL, data = phases_aov)
# aggregate data per condition and participant
phases_aov <- aggregate(. ~ participant + tool + dual, median, na.rm = TRUE, na.action = NULL, data = phaseDurations)
# try mancova approach which may be better
# aggregate data per condition and participant
phases_mancova <- aggregate(. ~ participant + tool + dual, median, na.rm = TRUE, na.action = NULL, data = phaseDurations)
phases_mancova$dual <- NULL
# run mancova
mancovaDuration <- manova(cbind(reach, ballApproach, ballGrasp, transport, slotApproach, slotEntry) ~
tool, data = phases_mancova)
summary(mancovaDuration)
manova(cbind(reach, ballApproach, ballGrasp, transport, slotApproach, slotEntry) ~
tool, data = phases_mancova)
mancovaDuration$coefficients
mancovaDuration$effects
mancovaDuration$rank
mancovaDuration$residuals
View(mancovaDuration)
mancovaDuration$qr
library("R.matlab")
# set directory
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
# load in data
phaseDurations = data.frame(readMat("fixationPatternPhases_new.mat"))
colnames(phaseDurations) <- c("testID", "participant", "fixationType",
"reach", "ballApproach", "ballGrasp",
"transport", "slotApproach", "slotEntry")
phaseDurations$participant <- as.factor(phaseDurations$participant)
phaseDurations$testID <- as.factor(phaseDurations$testID)
phaseDurations$fixationType <- as.factor(phaseDurations$fixationType)
# look at fingertips first
phaseDurations_FT <- phaseDurations[phaseDurations$testID == 3,]
phaseDurations_FT$testID <- NULL
# run mancova
mancovaFT <- manova(cbind(reach, ballApproach, ballGrasp, transport, slotApproach, slotEntry) ~
fixationType, data = phaseDurations_FT)
# look at tool trials
phaseDurations_TW <- phaseDurations[phaseDurations$testID == 4,]
phaseDurations_TW$testID <- NULL
# run mancova
mancovaTW <- manova(cbind(reach, ballApproach, ballGrasp, transport, slotApproach, slotEntry) ~
fixationType, data = phaseDurations_TW)
summary.aov(mancovaDuration)
# t-test for dual task condition
gazeShifts_dual <- gazeShifts[gazeShifts$dual == 1,]
t.test(gazeShifts_dual[gazeShifts_dual$tool == 1,]$gazeshift.slotDisplay, gazeShifts_dual[gazeShifts_dual$tool == 0,]$gazeshift.slotDisplay,
paired = TRUE)
rm(list = ls())
library("R.matlab")
# set directory
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
# load in data
coordination = data.frame(readMat("spatiotemporalCoordination.mat"))
colnames(coordination) <- c("participant", "testID", "tool", "dual",
"gazeShift.ballSlot", "gazeshift.slotDisplay")
coordination$participant <- as.factor(coordination$participant)
coordination$testID <- NULL #redundant
coordination$tool <- as.factor(coordination$tool)
coordination$dual <- as.factor(coordination$dual)
# aggregate data per condition and participant
gazeShifts <- aggregate(. ~ participant + tool + dual, median, na.rm = TRUE, na.action = NULL, data = coordination)
# t-test for dual task condition
gazeShifts_dual <- gazeShifts[gazeShifts$dual == 1,]
t.test(gazeShifts_dual[gazeShifts_dual$tool == 1,]$gazeshift.slotDisplay, gazeShifts_dual[gazeShifts_dual$tool == 0,]$gazeshift.slotDisplay,
paired = TRUE)
# mean and sd fingertips in ms
mean(gazeShifts_dual[gazeShifts_dual$tool == 0,]$gazeshift.slotDisplay)
# t-test for dual task condition
gazeShifts_dual <- gazeShifts[gazeShifts$dual == 1,]
# mean and sd fingertips in ms
mean(gazeShifts_dual[gazeShifts_dual$tool == 0,]$gazeshift.slotDisplay)
# mean and sd fingertips in ms
mean(gazeShifts_dual[gazeShifts_dual$tool == 0,]$gazeshift.slotDisplay, na.rm = TRUE)
View(gazeShifts_dual)
library("R.matlab")
# set directory
setwd(dirname(rstudioapi::getActiveDocumentContext()$path))
# load in data
coordination = data.frame(readMat("spatiotemporalCoordination.mat"))
colnames(coordination) <- c("participant", "testID", "tool", "dual",
"gazeShift.ballSlot", "gazeshift.slotDisplay")
coordination$participant <- as.factor(coordination$participant)
coordination$testID <- NULL #redundant
coordination$tool <- as.factor(coordination$tool)
coordination$dual <- as.factor(coordination$dual)
# aggregate data per condition and participant
gazeShifts <- aggregate(. ~ participant + tool + dual, median, na.rm = TRUE, na.action = NULL, data = coordination)
View(gazeShifts_dual)
# t-test for dual task condition
gazeShifts_dual <- gazeShifts[gazeShifts$dual == 1,]
View(gazeShifts_dual)
t.test(gazeShifts_dual[gazeShifts_dual$tool == 1,]$gazeshift.slotDisplay, gazeShifts_dual[gazeShifts_dual$tool == 0,]$gazeshift.slotDisplay,
paired = TRUE)
# mean and sd fingertips in ms
mean(gazeShifts_dual[gazeShifts_dual$tool == 0,]$gazeshift.slotDisplay, na.rm = TRUE)
sd(gazeShifts_dual[gazeShifts_dual$tool == 0,]$gazeshift.slotDisplay)
sd(gazeShifts_dual[gazeShifts_dual$tool == 0,]$gazeshift.slotDisplay, na.rm = TRUE)
View(gazeShifts_dual)
# mean & sd tweezers in ms
mean(gazeShifts_dual[gazeShifts_dual$tool == 1,]$gazeshift.slotDisplay)
rm(list = ls())
# load in data
coordination = data.frame(readMat("spatiotemporalCoordination.mat"))
colnames(coordination) <- c("participant", "testID", "tool", "dual",
"gazeShift.ballSlot", "gazeshift.slotDisplay")
coordination$participant <- as.factor(coordination$participant)
coordination$testID <- NULL #redundant
coordination$tool <- as.factor(coordination$tool)
coordination$dual <- as.factor(coordination$dual)
# aggregate data per condition and participant
gazeShifts <- aggregate(. ~ participant + tool + dual, median, na.rm = TRUE, na.action = NULL, data = coordination)
# t-test for dual task condition
gazeShifts_dual <- gazeShifts[gazeShifts$dual == 1,]
# mean and sd fingertips in ms
mean(gazeShifts_dual[gazeShifts_dual$tool == 0,]$gazeshift.slotDisplay, na.rm = TRUE)
sd(gazeShifts_dual[gazeShifts_dual$tool == 0,]$gazeshift.slotDisplay, na.rm = TRUE)
# mean & sd tweezers in ms
mean(gazeShifts_dual[gazeShifts_dual$tool == 1,]$gazeshift.slotDisplay)
sd(gazeShifts_dual[gazeShifts_dual$tool == 1,]$gazeshift.slotDisplay)
t.test(gazeShifts_dual[gazeShifts_dual$tool == 1,]$gazeshift.slotDisplay, gazeShifts_dual[gazeShifts_dual$tool == 0,]$gazeshift.slotDisplay,
paired = TRUE)
# mean and sd fingertips in ms
lastShifts <- aggregate(. ~ participant + dual, median, na.rm = TRUE, na.action = NULL, data = coordination)
mean(lastShifts$gazeshift.slotDisplay)
mean(lastShifts[lastShifts$dual == 1,]$gazeshift.slotDisplay, na.rm = TRUE)
sd(lastShifts[lastShifts$dual == 1,]$gazeshift.slotDisplay, na.rm = TRUE)
